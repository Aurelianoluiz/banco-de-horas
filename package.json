{
  "name": "banco-de-horas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check data-model.js && node --check integration.js && node --check rules/jornada.js && node --check rules/fechamento.js && node --check server.js"
  }
}
